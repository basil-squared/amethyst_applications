package basilsquared.amethyst_applications

import dev.architectury.registry.registries.DeferredRegister
import dev.architectury.registry.registries.RegistrySupplier
import net.minecraft.client.renderer.item.ItemProperties
import net.minecraft.core.registries.Registries
import net.minecraft.world.item.Item
import net.minecraft.world.level.ItemLike

object ModUtils {
    /* Register common properties that hardly differ item over item. Should be able to implement item type
        EG. Pickaxe, then it takes each given 'tier' (as of 1.0 let's only support vanillas :))
        and fills out the item fields, can probably also do smtn similar for datagen? probably

     */

    fun crystallineItemHelper(ItemType: String) {

    }






    }

